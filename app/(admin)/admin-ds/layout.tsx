import { HomeDoor, Journals, People } from "@/components/admin-components/icons";
import PageHeader from "@/components/admin-components//pageHeader";
import SideBar from "@/components/admin-components/sideBar";
import { ReduxProvider } from "@/redux/ReduxProvider";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
    title: "Admin",
    description: "Generated by create next app",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
    const navs = [
        {
            title: "Home",
            url: "/admin-ds/",
            icon: <HomeDoor width="20" height="20" />,
        },
        {
            title: "Users",
            url: "/admin-ds/users",
            icon: <People width="20" height="20" />,
        },
        {
            title: "Courses",
            url: "/admin-ds/courses",
            icon: <Journals width="20" height="20" />,
        },
    ];

    return (
        <html lang="en">
            <ReduxProvider>
                <body className={inter.className}>
                    <div className="d-flex">
                        <SideBar navs={navs} />
                        <div className="d-flex flex-column flex-grow-1 p-3">
                            <PageHeader title="Courses Menu" />
                            <div className="flex-grow-1">{children}</div>
                        </div>
                    </div>
                </body>
            </ReduxProvider>
        </html>
    );
}
